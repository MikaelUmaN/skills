name: validate

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install uv
        uses: astral-sh/setup-uv@v7
      - name: Validate marketplaces and skills
        run: uv run --frozen tools/validate_marketplaces.py
      - name: Run tests
        run: uv run --frozen --group dev pytest -q
